// Generated by CoffeeScript 1.6.3
(function() {
  var base, errorLogs, fs, mkdir, setUserSets, userSetting;

  fs = require('fs');

  mkdir = require('./lib/mkdir');

  base = {
    cwd: './',
    cfg: 'config.js'
  };

  errorLogs = {
    error: 0,
    warning: 0
  };

  userSetting = null;

  setUserSets = function(path) {
    var data;
    data = fs.readFileSync(path, 'utf-8');
    userSetting = JSON.parse(data);
  };

  exports.compile = function(cwd, file) {
    base.cwd = cwd || cwd.replace('\\', '/');
    base.cfg = file || base.cfg;
    setUserSets("" + base.cwd + "/" + base.cfg);
    console.log(userSetting);
  };

}).call(this);
