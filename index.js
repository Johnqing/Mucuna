// Generated by CoffeeScript 1.6.3
(function() {
  var CleanCSS, base, errorLogs, fs, pro, sysConfig, uglify;

  fs = require('fs');

  sysConfig = require('./config/sysConfig');

  CleanCSS = require('clean-css');

  uglify = require("uglify-js");

  pro = uglify.uglify;

  base = {
    cwd: './',
    cfg: 'config.js'
  };

  errorLogs = {
    error: 0,
    warning: 0
  };

  exports.compile = function(cwd, file) {
    base.cwd = cwd.replace('\\', '/');
    base.cfg = file || base.cfg;
    console.log(pro);
  };

}).call(this);
